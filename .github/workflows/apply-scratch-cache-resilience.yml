name: Apply scratch cache resilience

on:
  push:
    paths:
      - '.github/scratch_cache_resilience_patch.py'
      - '.github/workflows/apply-scratch-cache-resilience.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python .github/scratch_cache_resilience_patch.py
      - name: Commit source change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stem_dsp.cpp
          git commit -m "Keep scratch audio on cached trajectory"
          git push
